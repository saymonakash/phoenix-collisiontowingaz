---
import Header from "@/components/Header";
import Hero from "@/components/Hero";
import Services from "@/components/Services";
import CostCalculator from "@/components/CostCalculator";
import Contact from "@/components/Contact";
import Footer from "@/components/Footer";
import StickyCallButton from "@/components/StickyCallButton";
import Layout from "@/layouts/Layout.astro";

export const prerender = true;

const seoData = {
  title: "Towing Service in Phoenix, AZ | Collision Towing AZ",
  description: "Your local Phoenix towing service. 24/7 emergency towing and roadside assistance in Phoenix, Arizona. Get a free estimate now.",
  keywords: "towing service Phoenix, Phoenix towing, emergency towing Phoenix, roadside assistance Phoenix, tow truck Phoenix",
  canonicalUrl: "https://collisiontowingaz.com/",
};
---

<Layout {...seoData}>
  <div class="min-h-screen overflow-hidden">
    <Header client:load />
    <Hero client:load />
    <Services client:load />
    <CostCalculator client:load />
    <Contact client:load />
    <Footer client:load />
    <StickyCallButton client:load />
  </div>
</Layout>